@PACKAGE_INIT@

set_and_check(LazyKnightEngine_CMAKE_DIR "@PACKAGE_INSTALL_CONFIGDIR@")

if(NOT TARGET LazyKnight::Engine)
    include("${LazyKnightEngine_CMAKE_DIR}/LazyKnightEngineTargets.cmake")
endif()

set(LazyKnightEngine_LIBRARIES LazyKnight::Engine )

check_required_components(LazyKnight)